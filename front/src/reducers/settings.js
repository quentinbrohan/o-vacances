const initialState = {
  // ici l'état initial
  isDisabled: false,
};

const settings = (state = initialState, action = {}) => {
  switch (action.type) {
    default: return state;
  }
};

export default settings;
